# Copy to `.env` for docker-compose or local tools. Do not commit real secrets.
# Default passwords below are for local development only — change for any shared or production environment.

# --- AWS (used by lean-farm / proof pipelines when pointed at real resources) ---
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-west-2

# --- Claude / NLP ---
CLAUDE_API_KEY=
CLAUDE_MODEL=claude-3-opus-20240229

# --- Compose overrides (optional) ---
# POSTGRES_PASSWORD=
# REDIS_PASSWORD=
